#include "pch.h"
#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
	int n, ost[120], a, aost, amax = 0, bmax = 0;
	cin >> n;

	for(int i = 0; i < 120; i++)
		ost[i] = -1;

	for(int i = 0; i < n; i++)
	{
		cin >> a;
		aost = a % 120;

		if(aost == 0)
			aost = 120;

		if(ost[120 - aost] > a && ost[120 - aost] + a > amax + bmax)
		{
			amax = ost[120 - aost];
			bmax = a;
		}
		
		if(aost == 120)
			aost = 0;
		if (a > ost[aost])
			ost[aost] = a;
	}

	cout << amax << " " << bmax;
	return 0;
}
